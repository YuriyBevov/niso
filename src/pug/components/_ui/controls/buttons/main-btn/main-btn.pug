mixin main-btn(data)
  button.main-btn(type="button" class= data.cls ? data.cls : null)
    if data.icon
      img(src=`./assets/img/${data.icon}` alt= data.title width="40" height="40")
    if data.text
      span!= data.text